# Schedule
目前项目初步搭建，在解决方案里只保留了原 silk song 项目的引擎部分、project 的 Components部分，其他部分在解决方案里删除了，源文件部分删除部分没删除。
建议写 RPG 时另外打开 silk song 原项目做参考。
下面是schedule，需要开发者以 `Filename  **name**` 的形式列出自己做的部分。
如果该文件里有需要其他开发者协同开发的函数，则以二级目录的形式指出，如：
```
- file1  A
	- func1 need B
	- func2 need C
```

- engine/CollisionManager.cpp
- engine/CollisionManager.h
- engine/LevelManager.cpp
- engine/ResourceManager.cpp **wsx**
